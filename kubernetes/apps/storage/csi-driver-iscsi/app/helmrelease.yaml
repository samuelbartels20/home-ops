---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: csi-driver-iscsi
  namespace: storage
  annotations:
    meta.helm.sh/release-name: csi-driver-iscsi
    meta.helm.sh/release-namespace: storage
    fluxcd.io/automated: "true"
    fluxcd.io/tag.chart-image: glob:0.14.*
  labels:
    app.kubernetes.io/name: csi-driver-iscsi
    app.kubernetes.io/instance: csi-driver-iscsi
    app.kubernetes.io/component: storage
    app.kubernetes.io/part-of: storage
    app.kubernetes.io/version: "0.14.7"
    production-grade: "true"
    security-hardened: "true"

spec:
  interval: 1h
  timeout: 10m
  chart:
    spec:
      chart: democratic-csi
      version: 0.14.7
      sourceRef:
        kind: HelmRepository
        name: democratic-csi
        namespace: flux-system
  install:
    remediation:
      retries: 3
    crds: CreateReplace
  uninstall:
    keepHistory: false
  dependsOn:
    - name: external-secrets
      namespace: security
    - name: onepassword
      namespace: security

  values:
    # Secret configuration
    secrets:
      truenas-iscsi:
        create: false
        apiKey:
          secretRef:
            name: truenas-iscsi
            key: API_KEY
        decryptionKey:
          secretRef:
            name: truenas-iscsi
            key: DECRYPTION_KEY
    # Democratic CSI configuration
    driver:
      config:
        driver: zfs-generic-iscsi
        instance_id:
        httpConnection:
          protocol: https
          host:
            secretRef:
              name: truenas-iscsi
              key: host
          apiKey:
            secretRef:
              name: truenas-iscsi
              key: API_KEY
          decryptionKey:
            secretRef:
              name: truenas-iscsi
              key: DECRYPTION_KEY
          port: 443
          username:
            secretRef:
              name: truenas-iscsi
              key: username
          password:
            secretRef:
              name: truenas-iscsi
              key: password
          allowInsecure: true
        sshConnection:
          hostSecretKey: host
          port: 22
          usernameSecretKey: username
          passwordSecretKey: password
          secretName: truenas-iscsi
        zfs:
          cli:
            sudoEnabled: true
            chroot: /usr/sbin/chroot
            sudo: /usr/bin/sudo
            sudoPath: /sbin/sudo
            zfsPath: /sbin/zfs
            zpool: /sbin/zpool
            zpoolList: /usr/sbin/zpool
            zpoolImport: /usr/sbin/zpool
            zpoolExport: /usr/sbin/zpool
            zpoolCreate: /usr/sbin/zpool
            zpoolDestroy: /usr/sbin/zpool
            zpoolOnline: /usr/sbin/zpool
            zpoolOffline: /usr/sbin/zpool
            zpoolScrub: /usr/sbin/zpool
            zpoolSync: /usr/sbin/zpool
            zpoolResilver: /usr/sbin/zpool
            zpoolReplace: /usr/sbin/zpool
          datasetParentName: homelab/applications
          detachedSnapshotsDatasetParentName: homelab/snapshots
          zvolCompression: ""
          zvolDedup: ""
          zvolEnableReservation: false
          zvolBlocksize: 16K
        iscsi:
          targetPortal:
            secretRef:
              name: truenas-iscsi
              key: portal
          interface:
            namePrefix: csi-
            nameSuffix: "-cluster"
          targetGroups:
            - targetGroupPortalGroup: 2
              targetGroupInitiatorGroup: 2
              targetGroupAuthType: CHAP
              targetGroupAuthGroup: 2
          extentInsecureTpc: true
          extentXenCompat: false
          extentDisablePhysicalBlocksize: true
          extentBlocksize: 4096
          extentRpm: "7200"
          extentAvailThreshold: 0

    # CSI Driver configuration
    csiDriver:
      name: "org.democratic-csi.iscsi"
      attachRequired: true
      storageCapacity: true
      fsGroupPolicy: "File"

    # Controller configuration
    controller:
      enabled: true
      image:
        repository: democratic-csi/democratic-csi
        tag: v1.9.3
      strategy: deployment
      replicas: 2
      driver:
        # Resource configuration
        resources:
          limits:
            cpu: 500m
            memory: 512Mi
            ephemeral-storage: 1Gi
          requests:
            cpu: 50m
            memory: 64Mi
            ephemeral-storage: 100Mi

      # External provisioner sidecar
      externalProvisioner:
        enabled: true
        image: registry.k8s.io/sig-storage/csi-provisioner:v5.2.0
        logLevel: 2
        resources:
          limits:
            cpu: 300m
            memory: 256Mi
            ephemeral-storage: 1Gi
          requests:
            cpu: 30m
            memory: 32Mi
            ephemeral-storage: 100Mi

      # External resizer sidecar
      externalResizer:
        enabled: true
        image: registry.k8s.io/sig-storage/csi-resizer:v1.13.1
        logLevel: 2
        resources:
          limits:
            cpu: 200m
            memory: 256Mi
            ephemeral-storage: 1Gi
          requests:
            cpu: 20m
            memory: 32Mi
            ephemeral-storage: 100Mi

      # External snapshotter sidecar
      externalSnapshotter:
        enabled: true
        image: registry.k8s.io/sig-storage/csi-snapshotter:v8.2.0
        logLevel: 2
        resources:
          limits:
            cpu: 200m
            memory: 256Mi
            ephemeral-storage: 1Gi
          requests:
            cpu: 20m
            memory: 32Mi
            ephemeral-storage: 100Mi

      # Liveness probe sidecar
      livenessProbe:
        enabled: true
        image: registry.k8s.io/sig-storage/livenessprobe:v2.15.0
        resources:
          limits:
            cpu: 100m
            memory: 128Mi
            ephemeral-storage: 100Mi
          requests:
            cpu: 10m
            memory: 16Mi
            ephemeral-storage: 50Mi

      # Pod configuration
      podSecurityContext:
        runAsNonRoot: true
        runAsUser: 65534
        runAsGroup: 65534
        fsGroup: 65534
        seccompProfile:
          type: RuntimeDefault

      securityContext:
        allowPrivilegeEscalation: false
        capabilities:
          drop:
            - ALL
        readOnlyRootFilesystem: true
        runAsNonRoot: true
        runAsUser: 65534
        runAsGroup: 65534
        seccompProfile:
          type: RuntimeDefault

      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app.kubernetes.io/name
                      operator: In
                      values:
                        - csi-driver-iscsi
                topologyKey: kubernetes.io/hostname

      tolerations:
        - key: node-role.kubernetes.io/control-plane
          operator: Exists
          effect: NoSchedule
        - key: node-role.kubernetes.io/master
          operator: Exists
          effect: NoSchedule

      priorityClassName: system-cluster-critical

    # Node configuration
    node:
      mount:
        mount_flags: ""
        checkFilesystem:
          xfs:
            enabled: true
            customOptions: []
          ext4:
            enabled: true
            customOptions: []
            customFilesystemOptions: []
          btrfs:
            enabled: true
            customOptions: []
            customFilesystemOptions: []
      enabled: true
      image:
        repository: democratic-csi/democratic-csi
        tag: v1.9.3
      hostPID: true
      strategy: deployment
      driver:
        extraEnv:
        - name: ISCSIADM_HOST_STRATEGY
          value: nsenter
        - name: ISCSIADM_HOST_PATH
          value: /usr/local/sbin/iscsiadm
        iscsiDirHostPath: /etc/iscsi
        iscsiDirHostPathType: DirectoryOrCreate

        # Resource configuration
        resources:
          limits:
            cpu: 400m
            memory: 384Mi
            ephemeral-storage: 1Gi
          requests:
            cpu: 40m
            memory: 48Mi
            ephemeral-storage: 100Mi

      # Driver registrar sidecar
      driverRegistrar:
        enabled: true
        image: registry.k8s.io/sig-storage/csi-node-driver-registrar:v2.13.0
        logLevel: 2
        resources:
          limits:
            cpu: 100m
            memory: 128Mi
            ephemeral-storage: 100Mi
          requests:
            cpu: 10m
            memory: 16Mi
            ephemeral-storage: 50Mi

      # Liveness probe sidecar
      livenessProbe:
        enabled: true
        image: registry.k8s.io/sig-storage/livenessprobe:v2.15.0
        resources:
          limits:
            cpu: 100m
            memory: 128Mi
            ephemeral-storage: 100Mi
          requests:
            cpu: 10m
            memory: 16Mi
            ephemeral-storage: 50Mi

      # Pod configuration
      podSecurityContext:
        runAsNonRoot: false  # Required for iSCSI operations
        seccompProfile:
          type: RuntimeDefault

      securityContext:
        allowPrivilegeEscalation: true  # Required for iSCSI operations
        capabilities:
          add:
            - SYS_ADMIN  # Required for iSCSI mount operations
          drop:
            - ALL
        readOnlyRootFilesystem: true
        privileged: false
        seccompProfile:
          type: RuntimeDefault

      tolerations:
        - operator: Exists

      priorityClassName: system-node-critical

    # Storage classes
    storageClasses:
      - name: freenas-iscsi-csi
        defaultClass: false
        reclaimPolicy: Delete
        volumeBindingMode: Immediate
        allowVolumeExpansion: true
        parameters:
          fsType: ext4
        mountOptions: []
        secrets:
          provisioner-secret:
          controller-publish-secret:
          node-stage-secret:
            node-db.node.session.auth.authmethod: "CHAP"
            node-db.node.session.auth.username:
              secretRef:
                name: truenas-iscsi
                key: chap_username
            node-db.node.session.auth.password:
              secretRef:
                name: truenas-iscsi
                key: chap_password


    # Volume snapshot classes
    # volumeSnapshotClasses:
    #   - name: truenas-iscsi-snapclass
    #     driver: org.democratic-csi.iscsi
    #     annotations:
    #       snapshot.storage.kubernetes.io/is-default-class: "true"
    #     deletionPolicy: Retain
    #     parameters:
    #       detachedSnapshots: false

    # Service configuration for monitoring
    service:
      enabled: true
      type: ClusterIP
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9808"
        prometheus.io/path: "/metrics"

    # ServiceMonitor for Prometheus
    serviceMonitor:
      enabled: true
      labels:
        app.kubernetes.io/name: csi-driver-iscsi
      interval: 30s
      scrapeTimeout: 10s
      path: /metrics
      port: metrics

    # Pod annotations
    podAnnotations:
      prometheus.io/scrape: "true"
      prometheus.io/port: "9808"
      prometheus.io/path: "/metrics"
      reloader.stakater.com/auto: "true"

    # RBAC configuration
    rbac:
      create: true

    # Service account configuration
    serviceAccount:
      create: true
      annotations:
        app.kubernetes.io/managed-by: "Helm"
        app.kubernetes.io/component: storage

    # Image override for TrueNAS SCALE compatibility
    image:
      repository: democratic-csi/democratic-csi
      tag: v1.9.3