apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: postgres-vector-metrics
  namespace: storage
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: postgres-vector
      app.kubernetes.io/component: storage
  endpoints:
  - port: metrics
    interval: 30s
    path: /metrics